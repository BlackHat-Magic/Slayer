[project]
name = "slayer"
version = "0.1.0"
description = "Simple UI layout library"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.8",
    "ty>=0.0.40",
]
demo = [
    "pygame>=2.6.1",
]
