[project]
name = "slayer"
version = "0.1.0"
description = "Immediate-mode UI layout library"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame>=2.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.8",
    "ty>=0.0.40",
]
